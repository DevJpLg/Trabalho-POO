import { Router } from "express";

const produtoRoutes = Router();

// TODO: instanciar controller e registrar rotas
// produtoRoutes.post("/", controller.criar);
// produtoRoutes.get("/", controller.listar);
// produtoRoutes.get("/:id", controller.buscarPorId);
// produtoRoutes.put("/:id", controller.atualizar);
// produtoRoutes.delete("/:id", controller.remover);

export { produtoRoutes };
