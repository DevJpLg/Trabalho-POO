import { Router } from "express";

const estoqueRoutes = Router();

// TODO: instanciar controller e registrar rotas
// estoqueRoutes.get("/", controller.consultarEstoque);
// estoqueRoutes.post("/entrada", controller.registrarEntrada);
// estoqueRoutes.post("/saida", controller.registrarSaida);

export { estoqueRoutes };
